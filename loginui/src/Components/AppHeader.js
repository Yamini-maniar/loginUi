function AppHeader({title}){
    return(
        <div>
            <h1 class="App-header bg-gray-900 min-h-[20px] text-white text-center flex items-center uppercase">{title}</h1>
        </div>
    )
}

export default AppHeader;